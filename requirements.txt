# Core web framework
fastapi
uvicorn[standard]
python-multipart
python-dotenv

# LangChain ecosystem
langchain
langchain-community
langchain-core
langchain-openai
langchain-text-splitters

# PDF parsing
pypdf

# Vector store
faiss-cpu

# HTTP client (needed by openai/langchain)
httpx

# Numpy (required by faiss-cpu)
numpy

# Local embeddings (lightweight to fit in Render 512MB limit)
fastembed